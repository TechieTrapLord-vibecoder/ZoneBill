@{
    ViewData["Title"] = "Privacy Policy";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="fw-bold mb-0" style="color:var(--zb-text);">
        <i class="bi bi-shield-lock me-2" style="color:var(--zb-secondary);"></i>@ViewData["Title"]
    </h4>
</div>

<div class="zb-table-card">
    <div class="p-4">
        <p style="color:var(--zb-text-muted);">Use this page to detail your site's privacy policy.</p>
    </div>
</div>
